# Production Management (Next.js)

Scaffolded minimal Next.js TypeScript project with components, mock data, and pages.

Run locally:

```powershell
cd production-management
npm install
npm run dev
```

Open http://localhost:3002
